{
  "name": "quint-web-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal web starter for ONYX Quint / Splash MMP API — handoff for external frontend work",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
